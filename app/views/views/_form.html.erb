<%= form_with(model: view, local: true) do |form| %>
  <% if view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(view.errors.count, "error") %> prohibited this view from being saved:</h2>

      <ul>
        <% view.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :viewed_at %>
    <%= form.datetime_select :viewed_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
